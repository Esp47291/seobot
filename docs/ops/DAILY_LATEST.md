# Job Inside Daily

Дата: 2026-08-24 ~05:51 UTC. Run: H7 Copy system. Ветка: `docs/night-ops-20260824-00`.

### Недавно сделано (по коммитам/докам)

Код (origin/patch-15 `a768bf3`): без новой фичи.  
Docs H1–H6: UX, аналитика, лендинг 1 CTA, скрипты `#lead` / no-take, CM1 / касса, 15 касаний.  
Docs H7: инвентарь живого copy C-01…C-16; RULES без секрета; «на балансе» ≠ «оплачен»; **не слать prebuilt текст исполнителю**.

### Риски

- Продукт-of-record = **patch-15**, docs-ветка от **master**.
- `MemoryStorage`; ручные WD; `completed` ≠ `credited` у менеджера.
- Публичный copy читается как накрутка: RULES + пассив + «принят и оплачен».
- Prebuilt в `admin:allow` = диктовка отзыва в продукт, не только «не слать клиенту».
- Секрет прокси в git (`rules.py`) — не копировать в доки; ротация днём.
- Сайта нет; `#lead` некуда принимать (WEB-02).
- PR ночи не открыт (gh write нет).

### Топ-5 задач на сейчас (P0/P1/P2)

1. **P0** UX-01 /start: две роли (текст H7 готов)  
2. **P0** UX-02 Карточка: инструкция и доказательство до «Начать»  
3. **P0** UX-03 После сдачи: статус + срок; не «оплачен» до credited (C-05/C-06)  
4. **P0** TECH-01 Persist FSM — issue, не ночной рефакторинг  
5. **P1** SALES-01 15 касаний скриптом 1 в одном городе со supply; трекер take/no_take/hold  

Снято с топ-5: COPY-01 (spec-ready: тексты + стоп prebuilt). UNIT-01 / WEB-01 по-прежнему ждут поле/вёрстку. Не вытесняет UX P0.

### Одна рекомендация владельцу (1 предложение)

Завтра одним коротким PR текстов вырежьте из правил «платим за отзывы» и прокси, и перестаньте слать исполнителю готовый текст отзыва — это не копирайт, это граница площадок.

### Что сделано ЭТИМ run (инкремент)

- Research: тон×контекст, деньги=ETA, Maps UGC = не incentive и не диктовка.  
- COPY_SYSTEM: инвентарь as-is/to-be, RULES/канал/пусто, COPY-01 дневной чеклист.  
- Capability / gaps / TABLES 2b / RISKS: prebuilt исполнителю.  
- Код бота не менялся.
