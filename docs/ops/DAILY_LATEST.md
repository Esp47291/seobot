# Job Inside Daily

Дата: 2026-08-24 ~07:51 UTC. Run: H1 Product/UX (круг 2). Ветка: `docs/night-ops-20260824-00`.

### Недавно сделано (по коммитам/докам)

Код (origin/patch-15 `a768bf3`): без новой фичи.  
Docs H1–H8 круг 1: UX, аналитика, лендинг, скрипты, CM1, 15 касаний, copy C-01…C-16, inbox Q1–Q5.  
Docs H1 круг 2: UX-03 spec — id + стадия + рабочие часы; хаб ≠ позиция в очереди.

### Риски

- Продукт-of-record = **patch-15**, docs-ветка от **master**.
- `MemoryStorage`; ручные WD; `completed` ≠ `credited` у менеджера.
- Хаб `admission` без `login_screenshot`; очереди `id DESC`; Q5 unpaid нет в хабе.
- После скрина три «ожидайте» без id — отток в темноте; нельзя обещать N-го.
- Prebuilt в `admin:allow` = диктовка (Maps UGC / апр. 2026).
- Секрет прокси в git (`rules.py`) — не копировать в доки; ротация днём.
- Сайта нет; `#lead` некуда принимать (WEB-02).
- PR ночи не открыт (gh write нет).

### Топ-5 задач на сейчас (P0/P1/P2)

1. **P0** OPS-01 Inbox сегодня: SQL Q1–Q5 + не allow с prebuilt (`DAILY_INBOX.md`)  
2. **P0** UX-01 /start: две роли (текст H7 готов)  
3. **P0** UX-02 Карточка: инструкция до «Начать» + строка «проверка вручную, рабочие часы»  
4. **P0** TECH-01 Persist FSM — до фикса: deploy freeze при живой очереди  
5. **P0** COPY-01 + C-04 дневной PR текстов (spec UX-03 готов; код ночью не писать)  

Снято с топ-5: UX-03 **spec-ready**. SALES-01 остаётся **field**. UNIT-01 / WEB-01 ждут поле/вёрстку.

### Одна рекомендация владельцу (1 предложение)

После скрина пишите номер заявки и «обычно в рабочие часы», не минуты и не «вы в очереди» — хаб допуска занижает очередь, а фейковый срок сжигает доверие быстрее, чем само ожидание.

### Что сделано ЭТИМ run (инкремент)

- Research: фейковый countdown; ожидание в темноте (6 полей); статус заказа ≠ тикет.  
- UX-03 spec: стадии S2/S3, запрет N-го, шаблон C-04, рабочие часы без циферблата.  
- Journey 1.4a, TABLES wait states, Miro-карточки фрейма 2.  
- Код бота не менялся.
