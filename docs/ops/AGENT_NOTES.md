# Agent notes

## 2026-08-24 H3 (~01:49 UTC)

### Что получилось хорошо

- Сняли конкурирующий CTA «бот» с героя: это был главный баг черновика, не «нехватка текста».
- Оффер привязан к patch-15: менеджер создаёт TaskItem, кабинета нет, prebuilt тексты не на витрине.
- Форма 3 поля совпадает с каналом продаж (Telegram), не выдумывает CRM.

### Что слабо / не хватило данных

- Нет домена, приёма лидов, SLA ответа на заявку — форма пока бумажная.
- Городов supply нет: нельзя честно писать «работаем в N городах».
- Slack MCP нет. `gh pr create` штабу запрещён — ветка пушится, PR ночи владелец открывает сам.
- Visual/иконки площадок не трогали: не слабое место относительно героя.

### Правила на следующий час

- Не переписывать LANDING_BRIEF / EVENT_TAXONOMY / UX vision с нуля.
- Тема: **H4 Sales scripts**. Стык с формой: 4 вопроса брифа = поля + дожим в переписке.
- Не добавлять на сайт цены и «партнёр Яндекса».
- Дописывать ту же night-ветку. Не пушить master/main/patch-15.
- Не копировать секреты из rules.py.

### WHY_FAILED

не пустой run.

## 2026-08-24 H2 (~00:53 UTC)

### Что получилось хорошо

- Карта emit привязана к реальным функциям patch-15, не к абстрактной Mixpanel-схеме.
- Поймали ловушку: `review_ok` менеджерского слота ≠ `balance_update`; админский COUNT реф-операций ≠ cost.
- SQL-прокси дают владельцу что снять сегодня, не дожидаясь таблицы событий.

### Что слабо / не хватило данных

- Прод-БД по-прежнему нет: pass%, t-moderation, take→submit — формулы.
- `updated_at` как время решения — ASSUMPTION, не доказано.
- Slack MCP нет.
- PR ночи на момент письма ещё не был открыт H1 — открыть вместе с H2.

### Правила на следующий час

- Не переписывать EVENT_TAXONOMY / FUNNELS / UX vision с нуля.
- Тема: **H3 Website landing**.
- Не обещать на сайте автовыплаты, «гарантию рейтинга», реальную проверку подписки.
- Дописывать ту же night-ветку / тот же PR.
- Не копировать секреты из rules.py.
