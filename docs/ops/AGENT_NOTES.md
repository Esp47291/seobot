# Agent notes

## 2026-08-24 H2 (~00:53 UTC)

### Что получилось хорошо

- Карта emit привязана к реальным функциям patch-15, не к абстрактной Mixpanel-схеме.
- Поймали ловушку: `review_ok` менеджерского слота ≠ `balance_update`; админский COUNT реф-операций ≠ cost.
- SQL-прокси дают владельцу что снять сегодня, не дожидаясь таблицы событий.

### Что слабо / не хватило данных

- Прод-БД по-прежнему нет: pass%, t-moderation, take→submit — формулы.
- `updated_at` как время решения — ASSUMPTION, не доказано.
- Slack MCP нет.
- PR ночи на момент письма ещё не был открыт H1 — открыть вместе с H2.

### Правила на следующий час

- Не переписывать EVENT_TAXONOMY / FUNNELS / UX vision с нуля.
- Тема: **H3 Website landing**.
- Не обещать на сайте автовыплаты, «гарантию рейтинга», реальную проверку подписки.
- Дописывать ту же night-ветку / тот же PR.
- Не копировать секреты из rules.py.

### WHY_FAILED

не пустой run.

## 2026-08-24 H1 (00:23 UTC)

### Что получилось хорошо

- Не спутали master и patch-15: карта возможностей явно двухслойная.
- UX привязан к реальным статусам Attempt и экранам user.py, не к абстрактному «идеальному боту».
- Рефералка 20/5 сразу врезана в unit, чтобы продажи не обещали цену «от выплаты».

### Что слабо / не хватило данных

- Нет прод-БД: pass%, time-to-moderation, WD SLA — везде ASSUMPTION.
- Не читали admin.py целиком (1550+ строк) — broadcast/аналитика по grep.
- Slack MCP нет.
- Memories в начале run не существовали (первый проход).

### Правила на следующий час

- Не переписывать BOT_UX_VISION / USER_JOURNEY с нуля — только точечные правки, если нашли факт в коде.
- Тема: **H2 Analytics events** — углубить taxonomy + как врезать события в patch-15 без большого рефакторинга (псевдокод/места хендлеров).
- Не трогать код бота, если нет крошечного P0.
- Дописывать в ту же night-ветку / тот же PR, не плодить 8 PR.
- Не копировать секреты из rules.py (прокси) в доки.
