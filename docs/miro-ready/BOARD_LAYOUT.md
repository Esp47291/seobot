# Доска «Job Inside Business OS»

Порядок фреймов слева направо, сверху вниз. Завтра копипаст из TABLES.md и MIRO-READY секций файлов.

```
[0. Today] DAILY + топ-5 + рекомендация владельцу
[1. Product fact] Capability map (статус зон)
[2. UX] /start → задание → статус → кабинет → вывод
[2b. Copy] тон×слот + COPY-01 C-01…C-16 (не секрет прокси)
[3. Journeys] 3 дорожки: исполнитель / менеджер / админ
[4. Analytics] Taxonomy + воронки F1–F5 + карта «хендлер → emit» + SQL-прокси
[5. Unit] таблица экономии + оффер
[6. Site] герой (1 CTA) + форма 3 поля + IA без кабинета + площадки
[7. Sales] скрипты 1–16 + берём/не берём + стык `#lead`
[7b. Acquisition] каналы × ICP + трекер 15 + исходы qualify
[8. 30 days] недели 1–4
[9. Risks] ToS / фрод / касса / FSM / prebuilt-диктовка
[9b. Ops inbox] 5 полос Q1–Q5 + severity S0–S5 + ритуал 8 шагов (DAILY_INBOX)
[10. Sources] research (H1–H8)
```

Цвета фреймов (к VISUAL_DIRECTION): фон тёмный, акценты латунь = P0, мох = сделано.

## Карточки P0 на фрейме 0

1. /start две роли  
2. ТЗ до take  
3. SLA-текст после сдачи  
4. Persist FSM  
5. Inbox выплат + ToS-позиционирование  

## Не тащить на доску

Полные простыни handlers. Только таблицы и 1-строчные карточки.

Карточки на фрейм 4 (H2): «3 лога Phase 1»; «completed ≠ credited у менеджера»; «ref cost = SUM не COUNT»; «F1 окно 7д»; «supply/demand не мешать».

Карточки на фрейм 6 (H3): «1 CTA в герое»; «форма: имя / город / Telegram»; «нет кабинета заказчика»; «prebuilt тексты ≠ оффер сайта»; «бот только /executors».

Карточки на фрейм 7 (H4): «холод без ссылки»; «4 вопроса = форма + Q3/Q4»; «inbound не дублирует поля»; «накрутка → граница ToS»; «готовый текст → отказ»; «no-take без счёта».

Карточки на фрейм 5 (H5): «P_c нет в БД, price = P_ex»; «не take-rate, считать CM1»; «no_take ≠ дешёвый пилот»; «полы Google/2ГИС ≠ SKU»; «WD до оплаты клиента = нефондировано»; «скрипт 9 без ₽ до UNIT-01».

Карточки на фрейм 7b (H6): «15 × скрипт 1, 0 URL»; «город = supply first»; «sent→brief→take/no_take/hold»; «no_take не KPI-проигрыш»; «не затыкать слоты серым»; «партнёр ≠ local SEO ranking»; «чужой reply% почты не наш».

Карточки на фрейм 2 / 2b (H7): «правила ≠ оффер+прокси»; «две роли на /start»; «accepted ≠ оплачен»; «WD paid = сумма+остаток»; «реферал не пассив»; «не слать prebuilt текст»; «канал = скрипт 1».

Карточки на фрейм 2 (H1 круг 2 / UX-03): «не N-й — хаб admission врёт»; «стадия ≠ один “ожидайте”»; «ETA = рабочие часы, не 5 мин и не 24ч в ЛС»; «id + не дублировать кадр»; «оплачен ≠ этот шаг»; «таймер обеим сторонам — только после t_mod».

Карточки на фрейм 9 / 9b (H8): «хаб admission ≠ очередь (нет login_screenshot)»; «очереди id DESC прячут stale»; «Q5 mgr unpaid нет в хабе»; «WD ₽ только в extras»; «один SLA врёт — S0…S5»; «не allow с prebuilt»; «deploy freeze при живом FSM»; «нефондированный WD не ускорять».
